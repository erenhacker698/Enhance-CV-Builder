import { Section } from "@/lib/types"

interface SectionProps {
    section: Section
    isActive: boolean
    darkMode?: boolean
}

export default function ProjectsSection({ section, isActive, darkMode = false }: SectionProps) {
    return (
        <>
        </>
    )
}